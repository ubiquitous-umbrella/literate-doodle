version: 2.1

workflows:
  my-workflow:
    jobs:
      - run-tests:
          context:
            - global

jobs:
  run-tests:
    docker:
      - image: cimg/base:2020.01
    steps:
      - run:
          name: "echo environment variables from org-global context"
          command: echo $AWS_SECRET
